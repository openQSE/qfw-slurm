# qfw-slurm Manual Pages

The manual is the authoritative command and configuration reference. Sources
use conventional roff paths under `man/man<section>` and are installed through
CMake into `${CMAKE_INSTALL_MANDIR}`.

After installation, begin with:

```bash
man 7 qfw-slurm
```

Task-oriented test procedures remain under `docs/recipes`.
